/**
 Bubble Sort
 <Print # of swap operations>

 Shunji Lin
 10/12/2015
 **/

#include <iostream>
#include <vector>

int main() {
  int n; // number of entries
  cin >> n;

  vector<int> swap_array;

  for (int i = 0; i < n; i++) {
    int entry;
    cin >> entry;
    swap_array.push_back(entry);
  }
  
  //bubble sort
  for (int i = 0; i < n; i++) {
